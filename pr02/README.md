# Практическая работа №2
## Регулярные выражения, грамматики и языки

Вариант 13

---

## Постановка задачи

### Части 1-2

Язык L13 над алфавитом {0, 1}:

Все строки, заканчивающиеся на 010.

### Часть 3

Язык L39:

L39 = { 0^n 1^m 2^n | n, m >= 0 }

Требуется доказать, что L39 не является регулярным, используя лемму о разрастании.

---

## Содержимое репозитория

| Файл | Описание |
|------|----------|
| pr02.py | Python-реализация: ДКА, РВ, проверка L39, демонстрация леммы о разрастании |
| gen_jflap.py | Генератор JFLAP-файлов (.jff) |
| L13_DFA.jff | Детерминированный конечный автомат для L13 (JFLAP) |
| L13_Grammar.jff | Регулярная грамматика для L13 (JFLAP) |
| L13_RE.jff | Регулярное выражение для L13 (JFLAP) |
| README.md | Этот файл |

---

## Запуск

### Проверки на Python

    python pr02.py

Вывод:
- Проверка ДКА и РВ для L13 (сравнение результатов)
- Проверка принадлежности строк языку L39
- Демонстрация нарушения леммы о разрастании для L30 = {a^n b^l : n <= l}

### Генерация JFLAP-файлов

    python gen_jflap.py

Создаёт:
- L13_DFA.jff
- L13_Grammar.jff
- L13_RE.jff

Открывать в JFLAP: File -> Open -> выбрать .jff

---

## Результаты

### Часть 1-2: L13 = (0+1)*010

Регулярное выражение:

    (0+1)*010

ДКА:

| Состояние | 0 | 1 |
|-----------|---|---|
| -> q0 | q1 | q0 |
| q1 | q1 | q2 |
| q2 | q3 | q0 |
| q3 (final) | q1 | q0 |

Регулярная грамматика:

    S -> 0S | 1S | 0A
    A -> 1B
    B -> 0C
    C -> eps

Тесты:

| Строка | Результат |
|--------|-----------|
| 010 | Accept |
| 1010 | Accept |
| 0010 | Accept |
| 0101010 | Accept |
| 01 | Reject |
| 0110 | Reject |
| 111 | Reject |
| (пустая) | Reject |

---

### Часть 3: L39 = {0^n 1^m 2^n} — не регулярен

Доказательство (лемма о разрастании):

1. Предположим, L39 регулярен. Тогда существует p (длина накачки).
2. Возьмём s = 0^p 1^p 2^p. Заметим: s принадлежит L39, так как n = p, m = p, n0 = n2 = p. При этом |s| = 3p >= p.
3. По лемме о разрастании s = xyz, где |xy| <= p, |y| >= 1.
4. Так как |xy| <= p, а первые p символов строки s — это нули, то xy состоит только из нулей. Значит, y = 0^k, где k >= 1.
5. Рассмотрим строку xy^2z (накачка i = 2):

       xy^2z = 0^(p+k) 1^p 2^p

6. В этой строке количество нулей равно p + k, а количество двоек равно p. Так как k >= 1, получаем p + k != p. Условие 0^n 1^m 2^n нарушено. Следовательно, xy^2z не принадлежит L39.
7. Противоречие с леммой о разрастании. Значит, предположение неверно.
8. Вывод: L39 не является регулярным языком.

---

### Дополнительно: L30 = {a^n b^l : n <= l} — не регулярен

Доказательство (лемма о разрастании):

1. Предположим, L30 регулярен. Тогда существует p.
2. Возьмём s = a^p b^p. Заметим: s принадлежит L30, так как n = p, l = p, n <= l. При этом |s| = 2p >= p.
3. По лемме s = xyz, где |xy| <= p, |y| >= 1.
4. Так как |xy| <= p, а первые p символов — это a, то y = a^k, k >= 1.
5. Возьмём i = 2: xy^2z = a^(p+k) b^p.
6. Теперь n = p + k, l = p. Условие n <= l нарушено: p + k > p при k >= 1. Значит, xy^2z не принадлежит L30.
7. Противоречие. L30 не регулярен.

---

## Требования

- Python 3.8+
- JFLAP 7.1 (для открытия .jff файлов)

Внешние библиотеки не требуются. Используются только стандартные модули Python: re, xml.etree.ElementTree, xml.dom.minidom.
